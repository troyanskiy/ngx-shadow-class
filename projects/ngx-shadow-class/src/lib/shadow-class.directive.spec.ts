import { ShadowClassDirective } from './shadow-class.directive';

describe('ShadowClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowClassDirective();
    expect(directive).toBeTruthy();
  });
});
